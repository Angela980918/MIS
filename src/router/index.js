import {
	createRouter,
	createWebHashHistory
} from "vue-router";

// 路由配置
const routes = [{
	
}]

// 创建路由
const router = createRouter({
	history: createWebHashHistory(),
	routes: routes
})

export default router